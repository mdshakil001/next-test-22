import PropTypes from "prop-types";

const ARTICLE_PREVIEW_TYPE = {
  id: PropTypes.string,
  imageUrl: PropTypes.string,
  title: PropTypes.string,
  tags: PropTypes.array,
};

export { ARTICLE_PREVIEW_TYPE };
